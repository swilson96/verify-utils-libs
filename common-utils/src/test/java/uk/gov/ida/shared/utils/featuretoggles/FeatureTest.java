package uk.gov.ida.shared.utils.featuretoggles;

import org.junit.Test;

public class FeatureTest {
    @Test
    public void should_returnTrueWhenActiveStateIsSetToTrueInConfiguration() throws Exception {
//        FeatureConfiguration configuration = aFeatureConfiguration()
//                .withFeature(aFeatureEntry().withFeatureName(IdaFeatures.class).isActive(true).build())
//                .build();
//        FeatureRepository featureRepository = new FeatureRepository();
//        featureRepository.loadFeatures(configuration);
//
//        assertThat(IdaFeatures.isActive()).isTrue();
    }

    @Test
    public void should_returnFalseWhenActiveStateIsSetToFalseInConfiguration() throws Exception {
//        FeatureConfiguration configuration = aFeatureConfiguration()
//                .withFeature(aFeatureEntry().withFeatureName(IdaFeatures.class).isActive(false).build())
//                .build();
//        FeatureRepository featureRepository = new FeatureRepository();
//        featureRepository.loadFeatures(configuration);
//
//        assertThat(IdaFeatures.UIRework.isActive()).isFalse();
//
//        FeatureRepository.isActive(IdaFeatures.class);
//        myFeatureRepository.feature("Feature").isActive();
    }
}
